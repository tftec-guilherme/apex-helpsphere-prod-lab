{
  "name": "helpsphere-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "E2E smoke tests Lab Intermediário RAG — Playwright + Chrome do usuário (perfil persistido)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:smoke": "playwright test smoke-lab-inter.spec.ts --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "playwright": "^1.60.0"
  }
}
